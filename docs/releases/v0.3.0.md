# fcp-mcp v0.3.0 pre-release product evidence

This is fresh local pre-release evidence for `fcp-mcp` 0.3.0: built and
separately installed artifacts, a real installed-tool FCPXML generation, and a
real Final Cut Pro handoff. It is not a publication, tag, push, or claim of
autonomous Final Cut Pro control. Exact commands, paths, hashes, and result
bindings are in [v0.3.0-provenance.json](v0.3.0-provenance.json).

## Evidence identity

- Fixed code head installed for generation:
  `a15fc4683c2b2bf89f140321f74a89b3bcde93cc`
  (`codex/v0.3-product-polish`). The contract implementation commits are
  `06601df` and `a15fc46`.
- The earlier installed workflow-smoke evidence remains bound to source
  revision `0fe558be0040422701a75c67cdf04c9a8cbac253`; it is not used for the
  installed-generation claim below.
- Runtime: macOS 26.5.2 (build 25F84), Final Cut Pro 12.2
  (`com.apple.FinalCut`), Python 3.12.13.
- Installed catalog remains `inspect=30/2/0`, `workflow=34/3/3`,
  `edit=75/5/3`, `full=94/5/3` (tools/prompts/resources).

## Installed build sets

All four distributions below passed `twine check`. The fixed-head wheel is the
only artifact used to generate the live-import XML.

| Build | Artifact | SHA-256 |
| --- | --- | --- |
| Earlier workflow-smoke revision | wheel | `527b7cc46789125168a84999569c81e866804559e7dfa2a07a4c924d2612c7fb` |
| Earlier workflow-smoke revision | sdist | `cafd4ce4319723c6992b2fc4228e1fac503fb5a618da80b0d3845673cc7be2b8` |
| Fixed code head installed for generation | wheel | `b85e1ca6690570f80f9981b085d4e410f073fe4b711b071eeab838dfbf73424d` |
| Fixed code head installed for generation | sdist | `dc45927f5aafcf6149db65a1ef64aab01c376567313263b1e30a9f4bdd3901e4` |

The fixed wheel at
`/private/tmp/fcp-mcp-v030-evidence-r2/dist/fcp_mcp-0.3.0-py3-none-any.whl`
was installed into a fresh Python 3.12 virtual environment. Its
`direct_url.json` binds that absolute path and wheel hash. Provenance records
the exact console script, resolved interpreter and hash, site-packages import,
cwd, environment, and stdio argv.

## Earlier installed workflow trajectory

The retained earlier Python 3.12 workflow run is
`790da06a-04f7-48e8-a9e8-78014a464f6c`. It used source SHA-256
`5e0cfbe2647123c21d53c00ba75174736e7e127d2b26d0fb52722092242564d4`
and plan SHA-256
`79f29fcedf1cdc5ca645b1445bef525b5b6ec84998501458c238ff6a51d5bf0a`.
The two reviewed operations added marker `Wheel smoke` to clip `Clip` and
assigned role `Dialogue`.

- Candidate and committed output:
  `782f96039ed8a14e68bbbbf5649d719e3e81f8ef7c90a7c3dc2d74d7cb9bb3ff`.
- Diff artifact:
  `c3303bd83dccc021b9817f0606d44eca79e8915dbcbbdcd45a331f7e3403d71a`.
- Logical receipt hash:
  `d93bc297999668f9d1ff6b5da7ee484a6fb3a921db9479c3a86ad7ad657adc8b`.
- Serialized receipt artifact and ledger `receipt_sha256`:
  `2adc2bc2b7fc8795671ac5ce263a8a868e6ad16f4692c41363040db2d94b7d32`.

SQLite `integrity_check` returned `ok`; the event chain terminates in
`committed` at sequence 10. The materialized packaged sample remains a
separate check, not this workflow source.

## Installed generation and Final Cut Pro handoff

The authorized original was `/Volumes/Extreme SSD/GHG/P1010413.MP4`:
2,252,375,418 bytes, SHA-256
`b51ebe8fa82bc452ea882292d955808b01af11c572925c5785c94fca152308d1`.
The XML referenced that original source. During import, Final Cut Pro then
created a managed library copy at
`v0.3 Fixed Head Evidence/Original Media/P1010413.mp4`; the copy is also
2,252,375,418 bytes and has the same SHA-256.

Using the fixed wheel's installed stdio command, the operator called
`fcpxml_create_timeline` with source start `900900/30000s`, clip duration
`150150/30000s`, and explicit full asset duration `5735730/30000s`. The
retained machine-readable invocation record is
`/private/tmp/fcp-mcp-v030-evidence-r2/live/installed-generation.json`
(SHA-256
`8d2772be89007a04a324860c7b393b2afa1234570ed4878cac530d7047e17531`).
The installed tool generated
`/private/tmp/fcp-mcp-v030-evidence-r2/live/v0.3-installed-generated.fcpxml`
as 999 bytes with SHA-256
`2ad92d13768b28c123422beba718ed48c4146cc52f13f304c15292e2f5dc4c84`.
No manual XML edit occurred after generation.

The installed tool's typed receipt retained its internal
`Asset 'GHG P1010413 five-second evidence' has no source path` validation
warning. That warning is not hidden or treated as DTD evidence. The generated
XML's `media-rep` contains the authorized file URL; the unchanged bytes passed
the lxml-based `scripts/apple_dtd_gate.py` against Final Cut Pro's installed
1.11 DTD. No `xmllint --valid` success is claimed: the XML has no external
SYSTEM identifier, and direct macOS `xmllint --dtdvalid` retains the
`xmlSAX2ResolveEntity` caveat.

Using Final Cut Pro's native File > Import > XML dialog and Go to Folder, the
operator selected that exact generated path while the distinct disposable
`v0.3-installed-generated-evidence.fcpbundle` was active. Import returned
without a warning dialog; this is an operator observation, not screenshot
proof. The operator opened event `v0.3 Fixed Head Evidence` and project
`v0.3 Installed Generated Evidence`. Final Cut Pro visibly reported a
five-second timeline, video filmstrip, audio waveform, `1080p HD 29.97p,
Stereo`, and `Dialogue-1`.

![Installed-generated five-second FCP timeline with waveform and Dialogue role](../assets/v0.3-live-import.png)

The image is a genuine Final Cut Pro capture, cropped only: 972x768 PNG,
SHA-256 `a09e7460d8a96ba8415bb42c2e09ccd09645adc6a8a11d6946e92b7839ce993a`.
The privacy-safe crop preserves the fixed project identity, duration,
filmstrip, waveform, format, and role while excluding local paths and the
library sidebar.

## Inventory build and bounded validation

A final post-evidence wheel and sdist are built only to check package
inventory. They are neither installed nor published. Because a provenance
file cannot contain the hash of an archive containing that same final byte
sequence, the inventory artifacts embed a pre-attestation snapshot; the
public note/provenance append archive and snapshot hashes after construction.

| Final inventory-only artifact | SHA-256 |
| --- | --- |
| `fcp_mcp-0.3.0-py3-none-any.whl` | `7bc6125a409ff452ced1418adc556d78938c7cd357560a48ba25a6a83e909c5b` |
| `fcp_mcp-0.3.0.tar.gz` | `d979239dcf210f86c6f83bd32e499c3b3cc2957cebe18f9bde2767ed3bea01da` |

The inventory sdist contains pre-attestation note
`e6073551a51d449ba5a9f62435a403a4d6fe9a79c32972757e7ad0d75e3af81c`,
provenance
`4495eb9942a769e3aaf56c5a70c62c8a40ab6f377508b8de2848226820568588`,
and PNG
`a09e7460d8a96ba8415bb42c2e09ccd09645adc6a8a11d6946e92b7839ce993a`;
the retained member-hash comparison passed.

Strict TDD captured six initial asset-duration failures and two cached/conflict
edge failures before implementation. The focused generator, tool, result,
legacy-schema, and four-profile contract set then passed 132 tests. Bounded
release-evidence/product contracts, documented calls, DTD validation, both
build sets' `twine check`, inventory membership, PNG signature/dimensions/hash,
JSON parsing, and `git diff --check` are recorded with exact argv/results in
provenance.

## Boundaries

This proves one FCP 12.2 import of one simple installed-generated, DTD-valid
external-media clip into a disposable library. It does not establish
all-version compatibility, complex native-library features, collaboration
libraries, media relinking, or unattended editing. Live behavior remains
separately opt-in; physical outcomes are never inferred from a tool result.
CI was not triggered by this pre-release evidence task.
