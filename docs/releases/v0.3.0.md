# fcp-mcp v0.3.0 pre-release product evidence

This is fresh local pre-release evidence for `fcp-mcp` 0.3.0: a built and
separately installed artifact plus a real Final Cut Pro handoff. It is not a
publication, tag, or claim of autonomous Final Cut Pro control.

## Evidence identity

- Evidence source revision: `0fe558be0040422701a75c67cdf04c9a8cbac253`
  (`codex/v0.3-product-polish`), before this evidence-document commit.
- Runtime: macOS 26.5.2 (build 25F84), Final Cut Pro 12.2
  (`com.apple.FinalCut`), Python 3.14.6.
- DTD: `/Applications/Final Cut Pro.app/Contents/Frameworks/Interchange.framework/Versions/A/Resources/FCPXMLv1_11.dtd`.
- Installed catalog: `inspect=30/2/0`, `workflow=34/3/3`, `edit=75/5/3`,
  `full=94/5/3` (tools/prompts/resources). Default remains inspect, prepare,
  review, hash approval, then commit.
- Documentation naming macOS 15.6 records an earlier baseline; this evidence
  is specifically macOS 26.5.2 and does not relabel it.

## Built and installed artifact

`python -m build` and `twine check` passed at the evidence revision:

| Artifact | SHA-256 |
| --- | --- |
| `fcp_mcp-0.3.0-py3-none-any.whl` | `5e12aa751fba639f87e434eddb1487f481ea30d8a96714ae0a4fad89173b1993` |
| `fcp_mcp-0.3.0.tar.gz` | `9f1290863610ab28fcb6255cb7b9fdc9706848cdbf827840570e9b69791ca56d` |

The wheel installed with dependencies in a new temporary Python 3.14 venv;
its installed `fcp-mcp --version` returned `fcp-mcp 0.3.0`. Installed doctor
validated configuration, private write probe, and workflow ledger. It was
`degraded` only because optional Apple Compressor was absent; Final Cut Pro and
FFmpeg/ffprobe were present. The default doctor run kept live control disabled.

## Installed first-run trajectory

The installed command materialized the packaged sample, then the installed
workflow server completed the documented marker-only prepare/diff/approval/
commit/validate trajectory:

- Source SHA-256: `c54521e017d140cdf059fd950c7c286f0182e9622cd74ecacdfb83436b97ec02`.
- Run: `8806a5c5-affd-4d9d-b850-caf3b43e060b`; plan:
  `78a46c456fb92d5231f62bd016fcb12bd937a6015405e86dfd36e8340aa66809`.
- Candidate and committed output:
  `e8ef0ff17ba3d7043026d6b2a40920da6d2c92f9595a8c214b6d4ac9b3db93ee`.
- Diff: `0d2fb0f15804f2465c147cddf0c7c3fd0820a0860c8ad93a43d50124e37c46f5`.
- Receipt: `92d64b90c82c7745bc6d9adf82eb594e7c8ffdb4df3a93098d06cd95bce5391e`.

The reviewed diff contains exactly `First-run review marker` on `Demo Clip`;
prepare left the destination absent and commit bound the exact candidate hash.
Validation returned `valid: true`, with the expected offline-sample warning
that `Demo Clip` has no source path.

## Real-media Final Cut Pro handoff

The authorized, not copied source was `/Volumes/Extreme SSD/GHG/P1010413.MP4`:
2,252,375,418 bytes; SHA-256
`b51ebe8fa82bc452ea882292d955808b01af11c572925c5785c94fca152308d1`.
`ffprobe` found H.264 3840x2160 at `30000/1001`, stereo 48 kHz AAC, and
191.191 seconds.

The installed full-profile server generated `v0.3 Live Import Evidence` from
start `900900/30000s` for `150150/30000s` (5.005 seconds / 150 frames). Its
FCPXML SHA-256 is
`38fb38a0fd790ef8304c34db17a8a17558917f88cb065f906d9b116e9da553ff`.
`scripts/apple_dtd_gate.py` passed against Final Cut Pro's installed 1.11 DTD.
`xmllint --valid` with that DTD declared in the document exited 0; macOS
libxml's direct `--dtdvalid` emitted `xmlSAX2ResolveEntity`, so the lxml gate
is the authoritative DTD result.

Final Cut Pro created only the disposable
`v0.3-live-import-evidence.fcpbundle` / `v0.3 Evidence` event. Import returned
directly to that event with no FCP warning dialog. The opened project shows one
`00:00:05:00` AV clip, video filmstrip, audio waveform, 1080p HD 29.97p stereo,
and `Dialogue-1` in Audio Configuration, independently confirming duration
and audio presence.

![Imported five-second FCP timeline with waveform and Dialogue role](../assets/v0.3-live-import.png)

The image is a cropped, not reconstructed FCP screenshot: 950x768 pixels,
SHA-256 `362664b1a05948ed37e176fa19bab2c6a2036c6a5700857654aa4d5a87c27da0`.
It shows only the evidence project/timeline and no local path or unrelated
library/project sidebar.

## Boundaries

Targeted local gates passed: 17 product/release/documentation tests, 51
documented tool-call blocks, `twine check` for both final distributions,
sdist inventory for this release note and PNG, image dimensions (950x768), and
`git diff --check`.

This evidence proves one FCP 12.2 import of one simple DTD-valid external-media
clip into a disposable library. It does not establish all-version compatibility,
complex native-library features, collaboration libraries, media relinking, or
unattended editing. The product remains an offline reviewable FCPXML workflow;
live behavior is separately opt-in and physical outcomes are never inferred
from a tool result. CI was not triggered by this pre-release evidence task.
