# fcp-mcp v0.3.0 pre-release product evidence

This is fresh local pre-release evidence for `fcp-mcp` 0.3.0: an exact-head
source archive, separately installed wheel, installed-console workflow and
generation runs, Apple DTD validation, and a real Final Cut Pro import. It is
not a publication, tag, push, Registry submission, or claim of autonomous
Final Cut Pro control. Machine-readable paths, hashes, and result bindings are
in [v0.3.0-provenance.json](v0.3.0-provenance.json).

## Evidence identity

- Exact source commit:
  `9bcdaad157c1000b51c7c6b08df5ed012bc33e25` on
  `codex/v0.3-product-polish`.
- Source was clean before evidence collection and was exported with
  `git archive`; the installed build did not come from the mutable worktree.
- Runtime: macOS 26.5.2 (build 25F84), Final Cut Pro 12.2 build 447037,
  Python 3.12.13.
- Installed catalogs: `inspect=30/2/0`, `workflow=34/3/3`, `edit=75/5/3`,
  `full=94/5/3` (tools/prompts/resources). Doctor was non-error `degraded`
  because live control was deliberately disabled and Compressor is absent.

## Exact-head installed build

The archive was built under
`/private/tmp/fcp-mcp-v030-final-20260811T051833Z-69610/source`; both artifacts
passed `twine check`.

| Installed-build artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `fcp_mcp-0.3.0-py3-none-any.whl` | 240,999 | `6cb8c5f58219dca65ddf4407d59b6358af5527bdd1dbb6364816a1fc5ed57d74` |
| `fcp_mcp-0.3.0.tar.gz` | 909,882 | `c5ddea2e2927c198de43b33c9510af42b1844cb544229f83b13ffb6b53175f07` |

Only that wheel was installed into a fresh Python 3.12 environment. Its
`direct_url.json` SHA-256 is
`f988d7a2d6989c75417535e7e9bff82aebcc971ed26dd0366cb90bd4c55880f7`;
it names the same absolute wheel and same wheel digest. The installed console
script SHA-256 is
`02d143100fd3f9c1bab73fafabe85e77dbcc2a8b03951f3bee1519fd965a7cbc`.
A sorted 59-file installed-source manifest has SHA-256
`d128db11e2ad60277ae02f91eda941b19c2035d311ebcc425f509d08d410677c`.

An initial smoke attempt correctly failed before workflow initialization
because its state directory had mode 0755 rather than the required private
0700. After correcting only that evidence setup, the full installed-console
smoke passed. This is a fail-closed configuration check, not a product defect.

## Fresh packaged-sample transaction

The installed `sample` command materialized the packaged `Demo Clip` FCPXML
with SHA-256
`c54521e017d140cdf059fd950c7c286f0182e9622cd74ecacdfb83436b97ec02`.
The installed MCP console then performed a fresh prepare, review, exact-hash
approval, commit, and validate trajectory:

- Run ID: `22cda729-9d77-4708-bc13-f68fa9a1066d`.
- Plan SHA-256:
  `78a46c456fb92d5231f62bd016fcb12bd937a6015405e86dfd36e8340aa66809`.
- Candidate and committed output SHA-256:
  `e8ef0ff17ba3d7043026d6b2a40920da6d2c92f9595a8c214b6d4ac9b3db93ee`.
- Diff SHA-256:
  `57a11565c173be4332b16ba54260f1b608475c605b6cd4ee1d102405a72b056b`.
- Logical receipt SHA-256:
  `604e4e989c8126561a8f9dbad7a2d7525e06bb9fa751209345eca9e3832a2896`.
- Serialized receipt and ledger `receipt_sha256`:
  `635a6538b6d6bd773d5981045ac967958a0e00c946dd97b823854ac65ef74d79`.
- Ledger SHA-256:
  `22ae88c8011a5aab9440fb77e79c9757a9ac2ca5746fde9f142890c791d0a1a3`.

SQLite `integrity_check` returned `ok`; doctor checked one run and ten events
with no incomplete or recovery-required runs. The append-only chain terminates
in `committed` at sequence 10. The destination did not exist before commit.

## Installed generation and Final Cut Pro handoff

The authorized source was `/Volumes/Extreme SSD/GHG/P1010413.MP4`:
2,252,375,418 bytes, SHA-256
`b51ebe8fa82bc452ea882292d955808b01af11c572925c5785c94fca152308d1`.
Independent media inspection reported H.264 3840x2160 at 30000/1001, AAC
stereo at 48 kHz, duration 191.191 seconds.

Using only the installed console over MCP stdio, the operator called
`fcpxml_create_timeline` with source start `900900/30000s`, selected duration
`150150/30000s`, and explicit full asset duration `5735730/30000s`. The exact
invocation record has SHA-256
`3fa9c95877ccaf5804b36be2df311bd3317ea23bc529db7bfcfc42cdc851a079`.
The unchanged generated XML is 1,022 bytes with SHA-256
`a574c9d65a79983a214f558ef4dce650a737501afb7b0b6596de3146767f33eb`.
Its typed receipt retains the warning that the asset has no parser-level
source path; the generated `media-rep` itself contains the authorized file URL.

The unchanged bytes passed `scripts/apple_dtd_gate.py` against Final Cut Pro's
installed FCPXML 1.11 DTD. The DTD is 41,938 bytes with SHA-256
`fc85a0b4e56f28d63774f2e333301800db19c160b55cc118b55c4fe199f633be`;
the validation report SHA-256 is
`7368b4063c1dfe48ea004eda4abe6850f4b26d357015c5c3d66cd0da33307a7f`.

Using Final Cut Pro's native **File > Import > XML** route, the operator chose
that exact generated path while the distinct disposable library
`v0.3-final-head-evidence.fcpbundle` and its target event were active. The
macOS chooser required keyboard Right/Down selection before its Import button
became enabled; no warning dialog appeared after import. The exact project
`v0.3 Final Head Installed Evidence` opened in event
`v0.3 Final Head Evidence`. Final Cut Pro visibly reported a five-second clip,
video filmstrip, audio waveform, `1080p HD 29.97p, Stereo`, and `Dialogue-1`.

Final Cut Pro created a managed library copy at
`v0.3 Final Head Evidence/Original Media/P1010413.mp4`; it is also
2,252,375,418 bytes with SHA-256
`b51ebe8fa82bc452ea882292d955808b01af11c572925c5785c94fca152308d1`,
exactly matching the authorized original.

![Exact-head installed-generated five-second FCP timeline](../assets/v0.3-live-import.png)

The image is a genuine Final Cut Pro capture, cropped only: 972x768 PNG,
485,744 bytes, SHA-256
`5f77cd4a5a6ca953928fa47b509ef45e1b99b9dc91dde6c97772ef2ea9d284ac`.
The crop excludes the library sidebar and all local paths while retaining the
full project name, format, duration, filmstrip, waveform, and Dialogue role.

## Inventory-only self-reference boundary

After freezing the public note, machine-readable provenance, and screenshot,
an inventory-only wheel and sdist were built from the same exact source commit
with that pre-attestation snapshot overlaid. Both passed `twine check`:

| Inventory-only artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `fcp_mcp-0.3.0-py3-none-any.whl` | 240,999 | `45a62ad27e2d6466b322460dfb7b0ab76ac2890684709f2d105d7ca6b4f2e82b` |
| `fcp_mcp-0.3.0.tar.gz` | 911,187 | `6b6b22f9d1bdfdb7d203a72af7afa8d013f3434af07416c147e565cf161453d2` |

The sdist member hashes exactly matched the frozen inputs:

- `docs/releases/v0.3.0.md`:
  `91315704a9f55b141faba1f125147a45d153d3229452e9c3b13406794421c7f4`.
- `docs/releases/v0.3.0-provenance.json`:
  `5044aebe7dde26ec544cda679d56f0a0c1944f1a719ec2876a343121114fc1a7`.
- `docs/assets/v0.3-live-import.png`:
  `5f77cd4a5a6ca953928fa47b509ef45e1b99b9dc91dde6c97772ef2ea9d284ac`.

Those are pre-attestation snapshot hashes: the archive and snapshot digests in
this section were appended afterward, so the archive does not and cannot
contain its own final digest. The inventory-only wheel was not installed, and
neither inventory artifact was published. Runtime and live-import claims bind
only to the separately installed exact-head wheel above.

## Local verification

The final evidence files passed 31 focused release, product-surface,
documented-call, release-gate, and Apple-DTD tests. The full-profile contract
checker separately validated all 51 published tool-call blocks across its
three documentation files. JSON parsing, PNG signature/dimensions/size/hash,
both builds' `twine check`, inventory member hashes, the installed-Python DTD
rerun, Ruff on the changed Python test, and `git diff --check` also passed.
The authorized original and Final Cut Pro managed copy were byte-identical.
These are local results; this evidence task did not query or represent remote
CI status.

## Boundaries

This proves one Final Cut Pro 12.2 import of one simple installed-generated,
DTD-valid external-media clip into a disposable library. It does not establish
all-version compatibility, complex native-library features, collaboration
libraries, media relinking, or unattended editing. Live control remained
disabled, and physical outcomes were observed in Final Cut Pro rather than
inferred from a tool result.
